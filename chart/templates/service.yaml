apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoscaler-cloudscale.fullname" . }}
  labels:
    {{- include "autoscaler-cloudscale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "autoscaler-cloudscale.selectorLabels" . | nindent 4 }}
